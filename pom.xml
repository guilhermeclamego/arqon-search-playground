<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>io.github.guilhermeclamego</groupId>
    <artifactId>arqon-search-playground</artifactId>
    <version>1.0-SNAPSHOT</version>

    <name>ArqonSearch Playground</name>
    <description>Playground for ArqonSearch</description>

    <properties>
        <java.version>25</java.version>
        <arqon-search.version>1.0-SNAPSHOT</arqon-search.version>
        <elasticsearch.version>9.5.1</elasticsearch.version>
        <jackson.version>2.22.2</jackson.version>
    </properties>

    <dependencies>

        <!-- Spring Boot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>

        <!-- ArqonSearch -->
        <dependency>
            <groupId>io.github.guilhermeclamego</groupId>
            <artifactId>arqon-search-core</artifactId>
            <version>${arqon-search.version}</version>
        </dependency>

        <dependency>
            <groupId>io.github.guilhermeclamego</groupId>
            <artifactId>arqon-search-atlas</artifactId>
            <version>${arqon-search.version}</version>
        </dependency>

        <dependency>
            <groupId>io.github.guilhermeclamego</groupId>
            <artifactId>arqon-search-spring-boot</artifactId>
            <version>${arqon-search.version}</version>
        </dependency>

        <dependency>
            <groupId>org.mongodb</groupId>
            <artifactId>mongodb-driver-sync</artifactId>
        </dependency>

        <dependency>
            <groupId>co.elastic.clients</groupId>
            <artifactId>elasticsearch-java</artifactId>
            <version>${elasticsearch.version}</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>org.elasticsearch.client</groupId>
            <artifactId>elasticsearch-rest-client</artifactId>
            <version>${elasticsearch.version}</version>
        </dependency>

        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-core</artifactId>
            <version>${jackson.version}</version>
            <scope>compile</scope>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>1.18.46</version>
            <scope>compile</scope>
        </dependency>


        <!-- Tests -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>